COMPOSE_BAKE=true

# --- Infrastructure Settings ---
# Environment: 'development' (pretty logs) or 'production' (json logs)
ENVIRONMENT=development
LOG_LEVEL=INFO

# --- LLM Settings (vLLM) ---
# Model to serve. Ensure your GPU has enough VRAM (approx 60GB for 27B bf16).
MODEL_NAME=google/gemma-3-27b-it

# HuggingFace Token (Required for gated models like Gemma/Llama).
# Get it here: https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Internal API Key for vLLM <-> Agent communication
VLLM_API_KEY=local-dev-key

# --- Hardware Optimization (Tuned for NVIDIA A100 80GB) ---
GPU_MEMORY_UTILIZATION=0.95
MAX_MODEL_LEN=32768
DTYPE=bfloat16

# --- X (Twitter) Credentials ---
# Create a Project & App at https://developer.twitter.com/en/portal/dashboard
# IMPORANT: Ensure "User authentication settings" are set to "Read and Write".
API_KEY=your_consumer_key_here
API_KEY_SECRET=your_consumer_secret_here
ACCESS_TOKEN=your_access_token_here
ACCESS_TOKEN_SECRET=your_access_token_secret_here